# ADR-NNNN: short imperative title of the decision

<!--
  ADR template. To start a new ADR:
    1. Copy this file to docs/adr/NNNN-short-kebab-title.md, where NNNN is the
       next free number (see the index in adr/README.md). Numbers are never
       reused, even when an ADR is superseded.
    2. Delete this HTML comment and replace the title above.
    3. Fill in every section. Keep it honest: real options, real consequences.
    4. Add a row to the index in adr/README.md.
    5. If this decision introduces a new dependency, log the TA approval in
       docs/dependencies.md (process rule 8).
-->

- **Status:** Proposed <!-- Proposed | Accepted | Deferred | Superseded by ADR-NNNN -->
- **Date:** YYYY-MM-DD
- **Deciders:** <who made the call, e.g. "Whole team" or named leads>

## Context

What's the situation and why does a decision need making now? State the forces
at play: constraints, the existing ADRs this interacts with, and what's at
stake. End with the options considered:

- **Option A.** One line on what it is and its main trade-off.
- **Option B.** ...
- **Option C.** ...

## Decision

What did we decide, stated plainly. Be specific enough that a reader knows
exactly what to do (or not do) as a result. Reference the chosen option.

## Consequences

Positive:

- What gets better or easier.

Negative:

- What gets harder, what we're accepting, what we're giving up.

## When this ADR changes

The condition under which we'd revisit this and write a successor ADR.
